+   jQuery核心函数
    ```
        - 语法: $(), jQuery()

        - 核心函数可以接受多种参数,作用且不相同:
            1. 函数对象作为参数,是jQuery的入口函数;

            2.  选择器字符串作为参数,获取元素节点;

            3. 代码片段作为参数,会把其中的标签代码段封装成jQuery对象;

            4. DOM对象作为参数,会把DOM对象转成jQuery对象;

    ```