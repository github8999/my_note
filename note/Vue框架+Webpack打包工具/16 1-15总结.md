```
    1. MVC和MVVM的区别

    2. vue中的基本代码结构

    3. 插值表达式： v-cloak v-text v-html v-bind(缩写是:) v-on(缩写是@) v-model v-for v-if v-show

    4. 事件修饰符: .stop .prevent .capture .self .once

    5. el 指定要控制的区域, data 是个对象,指定了控制区域要用到的数据, methods是个对象,这里可以自定义方法

    6. 在VM实例中,如果需要访问data和methods中的数据和方法,必须带this

    7. 在V-for中要会使用key属性来确定数据的唯一性, key的值只能为number和string类型

    8. v-model 只作用于表单元素

    9. 在vue中绑定样式的两种方式 v-bind:class 和 v-bind:style
```